---
import LinkInline from "./LinkInline.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string,
  jobType: string
}

const { title, company, description, link, date, jobType } = Astro.props
---

<div
  class="relative mx-12 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-black/20 dark:before:border-white/15 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative pb-12 md:col-span-6">
    <div class="sticky top-0">
      <span class="text-emerald-400 -left-[42px] absolute rounded-full text-5xl"
        >&bull;</span
      >
      <div>
        <h3 class="text-xl font-bold text-emerald-400 inline-block pr-1">{title}</h3> / 
      <h4 class="font-thin  text-gray-600 dark:text-white inline-block pl-1">{company}</h4>
      </div>
      { 
        jobType === 'FT' ? (<span class="items-center rounded-md bg-emerald-50 dark:bg-cyan-900 px-2 py-1 text-xs font-medium text-emerald-700 dark:text-emerald-300 ring-1 ring-inset ring-emerald-600/20 inline-block">Tiempo completo</span>) : (<span class="items-center rounded-md bg-yellow-50 px-2 py-1 text-xs font-medium text-yellow-700 ring-1 ring-inset ring-yellow-600/20 inline-block">Freelance</span>)

      }
      
      <time class="p-0 m-0 text-sm text-gray-600/80 dark:text-white/80">{date}</time>
      <p class="text-dark-700 dark:text-dark-200 mt-5">{description}</p>
    </div>
  </div>
  <!-- <div class="relative flex flex-col gap-2 pb-4 text-gray-600 dark:text-gray-300 md:col-span-3">
    {description}
    {
      link && (
        <LinkInline href={link}>
          Saber más{" "}
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="w-5 icon icon-tabler icon-tabler-chevron-right"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            stroke-width="2"
            stroke="currentColor"
            fill="none"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 6l6 6l-6 6" />
            </>
          </svg>
        </LinkInline>
      )
    }
  </div> -->
</div>
